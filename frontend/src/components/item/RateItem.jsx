import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

function RateItem() {
    return (
        <Box>
            <Typography variant="h6">Rate Item</Typography>
        </Box>
    );
}

export default RateItem;
